(Дано натуральное число "n" необходимо проверить является ли число совершенным)
(Число является совершенным если сумма его делителей равна самому этому числу)
Более быстрый способ

#include "stdafx.h"
#include <iostream>
using namespace std;

int  SumDel(int n);
int _tmain(int argc, _TCHAR* argv[])
{
	setlocale(LC_ALL, "Russian");
	int n;
	cout<<"Введите число:";
	cin >> n;
	
	if (SumDel(n)==n){<-вот 
		cout << "Да\n";
	}
	else{
		cout << "Нет\n";
	}
	system("pause");
	return 0;
}
 int  SumDel(int n)
 {
	int sum = 1<-вот;
    for(int i = 2<-вот ; i <= n/2 ; i++)
	{
		if(n%i==0)
		sum+=i;
	}
    return sum;
 }