#include <iostream>
using namespace std;

int _tmain(int argc,_TCHAR* aggv[])
{
    setlocale(0, "");
    double num;

    cout << "Введите произвольное число: ";
    cin >> num;

    if (num < 10) { // Если введенное число меньше 10.
        cout << "Это число меньше 10." << endl;
	} else { // иначе
        cout << "Это число больше либо равно 10." << endl;
	}
    return 0;
}
в этой программе есть так называемое ветвление
эта программа сначало 1)вводит переменную "num"
2)выводит на экран надпись"Введите произвольное число:"(cout<<"...:"
3)просит вас задать значение переменной (cin >> num)
4)программа задает условие(if (num<10))если введенное вами число меньше 10,она выводит на экран надпись<<"Это число меньше 10"
5)если число меньше десяти выполняется второе условие (else{//иначе) это условие выводит на экран надпись<<"Это число больше либо равно 10"
